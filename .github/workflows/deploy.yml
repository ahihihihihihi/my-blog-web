name: Deploy to GitHub Pages

on:
  push:
    branches: ["main"]
  schedule:
    - cron: '0 */3 * * *' # Tự động chạy mỗi 3 tiếng
  workflow_dispatch:      # Nút bấm thủ công

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "18" # Đổi sang 18 để ổn định tuyệt đối
          cache: 'npm'
          
      - name: Install dependencies
        run: npm install --legacy-peer-deps # Thêm cờ này để tránh lỗi xung đột thư viện
        
      - name: Build with Next.js
        run: npm run build
        env:
          VITE_BLOGGER_ID: ${{ secrets.VITE_BLOGGER_ID }}
          
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4